import { StyleSheet } from 'react-native';

const friendsStyles = () =>
  StyleSheet.create({
    container: { flex: 1, backgroundColor: '#F9fafb', paddingHorizontal: 24,      paddingTop: 24,
 },
  });

export default friendsStyles;
