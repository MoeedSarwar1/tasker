import { scale } from 'react-native-size-matters';

export const fonts = {
  families: {
    primary: 'Poppins-Regular',
    medium: 'Poppins-Medium',
    bold: 'Poppins-Bold',
  },
  sizes: {
    xs: scale(12),
    sm: scale(14),
    md: scale(16),
    lg: scale(18),
    xl: scale(20),
    heading: scale(32),
  },
};
